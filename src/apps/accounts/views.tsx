import React from 'react'
import { MainProps, MainState } from './interfaces'

export default class LoginView extends React.Component<MainProps, MainState> {
  constructor(props: MainProps, state: MainState) {
    super(props)
  }

  render(): JSX.Element {
    return (<h1>Method not implemented.</h1>)
  }
}
